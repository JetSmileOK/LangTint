# Engineering evidence

Historical validation evidence lives here so the repository root stays product-focused.

These files document specific tested builds. They are not installation files and are not required to use LangTint. Current release integrity is enforced by CI and `tools/release.py`; interactive Explorer/DWM behavior still requires real Windows testing.
